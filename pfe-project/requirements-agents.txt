# Agent Service Dependencies
# Install with: pip install -r requirements-agents.txt

# ── Core web framework ─────────────────────────────────────────────────────────
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
pydantic>=2.0.0
pydantic-settings>=2.0.0

# ── HTTP client (ML tools call the existing FastAPI pipeline) ──────────────────
httpx>=0.27.0

# ── LangChain framework (locked decision #4) ──────────────────────────────────
langchain>=0.1.0
langchain-core>=0.1.0
langchain-community>=0.0.20

# ── LLM integration (Ollama for Mistral / Llama2 — locked decision #3) ────────
# Used for LLM-based reasoning when available; agents degrade gracefully without
langchain-ollama>=0.1.0

# ── Memory: Redis ─────────────────────────────────────────────────────────────
redis>=5.0.0

# ── Memory: SQLAlchemy (reads existing DB, writes agent tables) ───────────────
SQLAlchemy>=2.0.0

# ── Utilities ──────────────────────────────────────────────────────────────────
python-dotenv>=1.0.0
