# Annotation Bootstrap Assets

Generated artifacts in this folder support Month 1 annotation setup:
- `invoice_schema.json`: canonical field definitions
- `label_studio_config.xml`: importable Label Studio labeling configuration
- `annotation_guidelines.md`: annotator instructions and quality rules

These files can be regenerated from the Python bootstrap helpers in `app/annotation/bootstrap.py`.
